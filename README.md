

# Run the app locally

This contains everything you need to run the app on your machine.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
